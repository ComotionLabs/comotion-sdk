# comodash-dailyrun-api-client-lowlevel
Comotion Dash DailyRun API. These endpoints control and inspect the Daily ETL pipeline for a single Dash organisation.  They are served from the per-organisation frontend API rather than the main Dash `/v2` API, which is why they live in a separate specification and generate a separate client package. The organisation is determined by the hostname, so none of the operations take an organisation parameter. 

The `comodash_dailyrun_api_client_lowlevel` package is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0
- Package version: 1.0.0
- Generator version: 7.12.0
- Build package: org.openapitools.codegen.languages.PythonClientCodegen

## Requirements.

Python 3.8+

## Installation & Usage

This python library package is generated without supporting files like setup.py or requirements files

To be able to use it, you will need these dependencies in your own package that uses this library:

* urllib3 >= 1.25.3, < 3.0.0
* python-dateutil >= 2.8.2
* pydantic >= 2
* typing-extensions >= 4.7.1

## Getting Started

In your own code, to use this library to connect and interact with comodash-dailyrun-api-client-lowlevel,
you can run the following:

```python

import comodash_dailyrun_api_client_lowlevel
from comodash_dailyrun_api_client_lowlevel.rest import ApiException
from pprint import pprint

# Defining the host is optional and defaults to https://api.training.comodash.io/superset
# See configuration.py for a list of all supported configuration parameters.
configuration = comodash_dailyrun_api_client_lowlevel.Configuration(
    host = "https://api.training.comodash.io/superset"
)

# The client must configure the authentication and authorization parameters
# in accordance with the API server security policy.
# Examples for each auth method are provided below, use the example that
# satisfies your auth use case.

# Configure Bearer authorization (JWT): OAuth2Authorizer
configuration = comodash_dailyrun_api_client_lowlevel.Configuration(
    access_token = os.environ["BEARER_TOKEN"]
)


# Enter a context with an instance of the API client
with comodash_dailyrun_api_client_lowlevel.ApiClient(configuration) as api_client:
    # Create an instance of the API class
    api_instance = comodash_dailyrun_api_client_lowlevel.DailyRunApi(api_client)

    try:
        # Get whether the daily run is enabled
        api_response = api_instance.get_daily_run_enabled()
        print("The response of DailyRunApi->get_daily_run_enabled:\n")
        pprint(api_response)
    except ApiException as e:
        print("Exception when calling DailyRunApi->get_daily_run_enabled: %s\n" % e)

```

## Documentation for API Endpoints

All URIs are relative to *https://api.training.comodash.io/superset*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*DailyRunApi* | [**get_daily_run_enabled**](comodash_dailyrun_api_client_lowlevel/docs/DailyRunApi.md#get_daily_run_enabled) | **GET** /dailyRun/dailyRun_enabled | Get whether the daily run is enabled
*DailyRunApi* | [**get_daily_run_execution_status**](comodash_dailyrun_api_client_lowlevel/docs/DailyRunApi.md#get_daily_run_execution_status) | **GET** /dailyRun/execution_status | Get Daily ETL pipeline execution information
*DailyRunApi* | [**start_daily_run_execution**](comodash_dailyrun_api_client_lowlevel/docs/DailyRunApi.md#start_daily_run_execution) | **POST** /dailyRun/start_execution | Start a Daily ETL pipeline execution
*DailyRunApi* | [**update_daily_run_enabled**](comodash_dailyrun_api_client_lowlevel/docs/DailyRunApi.md#update_daily_run_enabled) | **POST** /dailyRun/dailyRun_enabled | Enable or disable the daily run


## Documentation For Models

 - [DailyRunEnabled](comodash_dailyrun_api_client_lowlevel/docs/DailyRunEnabled.md)
 - [Error](comodash_dailyrun_api_client_lowlevel/docs/Error.md)
 - [Execution](comodash_dailyrun_api_client_lowlevel/docs/Execution.md)
 - [ExecutionStatus](comodash_dailyrun_api_client_lowlevel/docs/ExecutionStatus.md)
 - [RunningExecution](comodash_dailyrun_api_client_lowlevel/docs/RunningExecution.md)
 - [StartExecutionResponse](comodash_dailyrun_api_client_lowlevel/docs/StartExecutionResponse.md)


<a id="documentation-for-authorization"></a>
## Documentation For Authorization


Authentication schemes defined for the API:
<a id="OAuth2Authorizer"></a>
### OAuth2Authorizer

- **Type**: Bearer authentication (JWT)


## Author




